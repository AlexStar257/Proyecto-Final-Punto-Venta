<%- include('./partials/main') %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-dark table-bordered table-hover">
        <thead>
          <tr>
            <td>ID</td>
            <td>Nombre</td>
            <td>Descripción</td>
            <td>Precio</td>
            <td>Imagen</td>
            <td>Disponibilidad</td>
            <td>Estado</td>
          </tr>
        </thead>
        <tbody>
          <% if (productos) { %> <% for(var i = 0; i < productos.length; i++) {
          %>
          <tr>
            <td><%= productos[i].id %></td>
            <td><%= productos[i].nombre %></td>
            <td><%= productos[i].descripcion %></td>
            <td><%= productos[i].precio %></td>
            <td><%= productos[i].urlImagen %></td>
            <td><%= productos[i].disponibilidad %></td>
            <td><%= productos[i].estado %></td>
            <td class="d-flex gap-2">
              <a href="/update/<%= productos[i].id %>" class="btn btn-info">
                Edit
              </a>
              <a href="/delete/<%= productos[i].id %>" class="btn btn-danger">
                Delete
              </a>
            </td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
    <div class="col-md-5">
      <div class="card-body">
        <form action="/add" method="POST">
          <input
            type="text"
            name="nombre"
            placeholder="Nombre"
            class="form-control mb-2"
            autofocus
          />
          <input
            type="text"
            name="descripcion"
            placeholder="Descripcion"
            class="form-control mb-2"
          />
          <input
            type="text"
            name="precio"
            placeholder="Precio"
            class="form-control mb-2"
          />
          <input
          type="file" 
          accept="image/png,image/jpeg" 
          id="urlImagen" 
          name="urlImagen"
          class="form-control mb-2"
          autofocus
        />
        <input
          type="number"
          name="disponibilidad"
          placeholder="Disponibilidad"
          class="form-control mb-2"
        />

          <button type="submit" class="btn btn-info">save customer</button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- <%- include("partials/_footer") %> -->